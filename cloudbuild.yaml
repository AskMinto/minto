steps:
  # ── Backend ───────────────────────────────────────────────────────────────
  - name: gcr.io/cloud-builders/docker
    id: build-api
    args:
      - build
      - -t
      - gcr.io/$PROJECT_ID/minto-api:$COMMIT_SHA
      - -f
      - backend/Dockerfile
      - backend

  - name: gcr.io/cloud-builders/docker
    id: push-api
    waitFor: [build-api]
    args:
      - push
      - gcr.io/$PROJECT_ID/minto-api:$COMMIT_SHA

  - name: gcr.io/google.com/cloudsdktool/cloud-sdk
    id: deploy-api
    waitFor: [push-api]
    entrypoint: gcloud
    args:
      - run
      - deploy
      - minto-api
      - --image
      - gcr.io/$PROJECT_ID/minto-api:$COMMIT_SHA
      - --region
      - asia-south1
      - '--set-env-vars'
      - >-
        SUPABASE_URL=https://yxnuoabbcxbkgjmluyez.supabase.co,
        SUPABASE_ANON_KEY=REDACTED_SUPABASE_ANON_KEY,
        SUPABASE_JWKS_URL=https://yxnuoabbcxbkgjmluyez.supabase.co/auth/v1/keys,
        SUPABASE_JWT_SECRET=REDACTED_SUPABASE_JWT_SECRET,
        GEMINI_API_KEY=REDACTED_GEMINI_API_KEY,
        MEM0_PROJECT_KEY=REDACTED_MEM0_PROJECT_KEY,
        MEM0_API_KEY=REDACTED_MEM0_API_KEY,
        YFINANCE_MAX_RESULTS=12,
        YFINANCE_NEWS_COUNT=6,
        KITE_API_KEY=REDACTED_KITE_API_KEY,
        KITE_API_SECRET=REDACTED_KITE_API_SECRET,
        KITE_REDIRECT_URL=minto://zerodha-callback,
        TWILIO_WHATSAPP_FROM=whatsapp:+14155238886,
        GCS_BUCKET_NAME=minto-wa-uploads
      - '--set-secrets'
      - 'OPENAI_API_KEY=projects/557303266732/secrets/OPENAI_API_KEY:latest,SUPABASE_SERVICE_ROLE_KEY=projects/557303266732/secrets/SUPABASE_SERVICE_ROLE_KEY:latest,TWILIO_ACCOUNT_SID=projects/557303266732/secrets/TWILIO_ACCOUNT_SID:latest,TWILIO_AUTH_TOKEN=projects/557303266732/secrets/TWILIO_AUTH_TOKEN:latest'
      - --platform
      - managed
      - --allow-unauthenticated
      - --port
      - "8080"

  # ── Frontend ──────────────────────────────────────────────────────────────
  # NEXT_PUBLIC_* vars are baked into the JS bundle at build time via ARGs.
  - name: gcr.io/cloud-builders/docker
    id: build-web
    args:
      - build
      - -t
      - gcr.io/$PROJECT_ID/minto-web:$COMMIT_SHA
      - -f
      - web-app/Dockerfile
      - --build-arg
      - NEXT_PUBLIC_SUPABASE_URL=https://yxnuoabbcxbkgjmluyez.supabase.co
      - --build-arg
      - NEXT_PUBLIC_SUPABASE_ANON_KEY=REDACTED_SUPABASE_ANON_KEY
      - --build-arg
      - NEXT_PUBLIC_API_BASE_URL=https://minto-api-557303266732.asia-south1.run.app
      - web-app

  - name: gcr.io/cloud-builders/docker
    id: push-web
    waitFor: [build-web]
    args:
      - push
      - gcr.io/$PROJECT_ID/minto-web:$COMMIT_SHA

  - name: gcr.io/google.com/cloudsdktool/cloud-sdk
    id: deploy-web
    waitFor: [push-web]
    entrypoint: gcloud
    args:
      - run
      - deploy
      - minto-web
      - --image
      - gcr.io/$PROJECT_ID/minto-web:$COMMIT_SHA
      - --region
      - asia-south1
      - --platform
      - managed
      - --allow-unauthenticated
      - --port
      - "3000"

images:
  - gcr.io/$PROJECT_ID/minto-api:$COMMIT_SHA
  - gcr.io/$PROJECT_ID/minto-web:$COMMIT_SHA
options:
  logging: CLOUD_LOGGING_ONLY
